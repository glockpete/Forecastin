# FastAPI and ASGI
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Database
psycopg[binary]==3.2.3
SQLAlchemy==2.0.44
asyncpg==0.29.0

# Serialization (CRITICAL: orjson required for WebSocket in main.py)
orjson==3.9.10
ujson==5.9.0

# Caching and Redis (L2 Cache Tier)
redis==5.0.1
hiredis==2.2.3

# HTTP Client and API
httpx==0.25.2
aiohttp==3.9.1

# WebSocket Support
websockets==12.0

# Configuration and Environment
pydantic==2.6.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# File Operations
aiofiles==23.2.1

# Date/Time Utilities
python-dateutil==2.8.2

# Logging and Monitoring
structlog==23.2.0
prometheus-client==0.14.1

# Testing Dependencies
pytest==8.3.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-benchmark==4.0.0

# Code Quality and Linting
black==23.12.1
isort==5.13.2
flake8==7.0.0
mypy==1.7.1
types-redis==4.6.0.11

# Security Scanning
bandit==1.7.5
safety==3.7.0  # Updated from 2.3.5 to resolve packaging dependency conflict with black
semgrep==1.45.0

# Pre-commit hooks
pre-commit==3.6.0

# Performance Testing
locust==2.42.2
memory-profiler==0.61.0
psutil==5.9.8

# Geospatial (for PostGIS integration)
shapely==2.1.2

# RSS Processing and HTML Parsing
beautifulsoup4==4.12.2
trafilatura==1.6.0

# Machine Learning and Forecasting (Optional - required for hierarchical forecasting)
# Uncomment these lines if you need Prophet-based forecasting features:
# prophet==1.1.5
# pandas==2.1.4
# numpy==1.26.2
# pyarrow==14.0.1
#
# Note: These packages require extensive build dependencies (gcc, g++, cmake, etc.)
# They are NOT required for basic API functionality.
# Install with: pip install prophet pandas numpy pyarrow
# Or use requirements.full.txt for all optional dependencies

# For development dependencies (testing, linting, security scanning, etc.):
# pip install -r requirements-dev.txt
